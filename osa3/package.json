{
  "name": "puhelinluettelobackend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Matti Kinnunen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && npm --prefix ../osa2/puhelinluettelo run build && cp -r ../osa2/puhelinluettelo/dist ./dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
